# duvet regenerates these on every `duvet report`. Committing them would
# duplicate the spec text and let it drift.
.duvet/reports/
.duvet/requirements/

node_modules/
.DS_Store

# Scratch space the review and coordination skills write into while they run —
# waiter logs, worktree state. Never part of a change.
.claude/team/
